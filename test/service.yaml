apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: test
spec:
  type: NodePort
  selector:
    app: nginx4
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 32000