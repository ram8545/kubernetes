apiVersion: v1
kind: Pod
metadata:
  name: nginx3
  labels:
    app: nginx3
    group: nginx
  namespace: test
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
    - containerPort: 80